// THE ONLY FILE THAT CHANGES BETWEEN ENVIRONMENTS
// Local dev  → API_URL = "http://localhost:8080"
// Production → API_URL = "https://your-deployed-api.com"

const CONFIG = {
  API_URL: "http://localhost:8080",
  VERSION: "v1",
};
